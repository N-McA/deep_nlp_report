
from setuptools import setup, find_packages

setup(
    name = 'deep_nlp_report',
    version = '0.0.1',
    url = 'git@github.com:N-McA/deep_nlp_report.git',
    author = 'Nat McAleese',
    packages = find_packages(),    
)
